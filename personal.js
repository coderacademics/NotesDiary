let i=0;
let password=prompt("Enter your password to proceed:");
while(i>=0){
    
    if(password!="saikat")
    {
        alert("Wrong password , Enter again!!");
        password=prompt("Enter your password to proceed:");
    }
    else{
        i=-1;
    }
}